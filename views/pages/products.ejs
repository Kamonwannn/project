<% include ../partials/header %>

            <table class="table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Title</th>
                        <th>Price</th>
                        <th>Edit</th>
                        <th><a name="" id="" class="btn btn-success" href="/insert_product/<%= myFunction()%>" role="button">Add Product</a></td></th> 
                    </tr> 
                </thead>
                <tbody id="myTable">
                    <% products.forEach(function(product){ %>
                    <tr>
                        <td scope="row"><%= product.id %></td>
                        <td><%= product.title %></td>
                        <td><%= product.price %></td>
                        <td><a class="btn btn-primary" href="/products/<%= product.id %>" role="button"> Edit </a></td>
                        <td><a name="" id="" class="btn btn-danger" href="/product_delete/<%= product.id %>" onclick="return confirm('Are you sure you want to delete?')" role="button">Delete</a></td>
         </tr>
         
         <% }); %>
     </tbody>
 </table>

 <script>
        function myFunction() {
            var d = new Date();
            d.setMonth(4);
            document.getElementById("demo").innerHTML = d;
        }
        </script>

<% include ../partials/footer %>